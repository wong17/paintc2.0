namespace Paintc.Shapes.CStructs
{
    /* 
     * putpixel (int x, int y, int col);
     * 
     * Dibuja un pixel (punto) en la posición (x,y) con el color col. 
     */

    public struct Pixel
    {
        public int x, y, color;
    }
}