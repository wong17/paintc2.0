<Window x:Class="Paintc.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:Paintc.ViewModels"
        xmlns:userControls="clr-namespace:Paintc.View.UserControls"
        mc:Ignorable="d"
        Title="Paintc2.0" Width="1920" Height="1080" WindowStartupLocation="CenterScreen">

    <!--ViewModel-->
    <Window.DataContext>
        <vm:MainWindowViewModel />
    </Window.DataContext>

    <!---->
    <Grid x:Name="WindowLayout">
        <!--Rows definition-->
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!--Menu-->
        <Menu Grid.Row="0" Style="{StaticResource MenuStyle}">
            <MenuItem Header="{StaticResource FileMenu}" Style="{StaticResource MenuItemStyle}">
                <MenuItem Header="{StaticResource ExitMenuItem}" Command="{Binding ExitCommand}" />
            </MenuItem>
            <MenuItem Header="{StaticResource HelpMenu}" Style="{StaticResource MenuItemStyle}">
                <MenuItem Header="{StaticResource AboutMenuItem}" Command="{Binding AboutCommand}" />
            </MenuItem>
        </Menu>

        <!--Content-->
        <Grid Grid.Row="1" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Height="Auto" Width="Auto">
            <!--Columns definition-->
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" MinWidth="200" MaxWidth="300"/>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" MinWidth="200" MaxWidth="300"/>
            </Grid.ColumnDefinitions>
            
            <!--Left panel-->
            <StackPanel Grid.Column="0" Background="{StaticResource BlackBrush}"/>
            <!--GridSplitter-->
            <GridSplitter Grid.Column="1" Width="2" ResizeBehavior="PreviousAndNext" />
            <!--Center panel-->
            <userControls:DrawingPanel Grid.Column="2"/>
            <!--GridSplitter-->
            <GridSplitter Grid.Column="3" Width="2" ResizeBehavior="PreviousAndNext" />
            <!--Right panel-->
            <StackPanel Grid.Column="4" Background="{StaticResource BlackBrush}"/>
        </Grid>

    </Grid>
</Window>
